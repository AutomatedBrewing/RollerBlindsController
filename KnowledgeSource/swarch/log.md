# Directory Update Log

## 2026-07-27

* **Creation**: Initial creation of the `swarch` (Software Architecture) knowledge bundle, filling the gap below SYSMOD's Product Architecture (SM-PRD-21), which SYSMOD explicitly places out of scope.
* **Creation**: 7 artifact definitions in `products/` — Software Architecture Constraints (SWA-01), Software Solution Strategy (SWA-02), Software Building Block View (SWA-03), Software Runtime View (SWA-04), Software Deployment View (SWA-05), Software Cross-cutting Concepts (SWA-06), Software Architecture Decisions (SWA-07) — organised per SW-relevant arc42 artifact rather than per SYSMOD product, since SYSMOD has no method/product pair at this level.
* **Creation**: 7 corresponding cross-mapping files in `crossmapping/`, each following the same §1 Definition / §2 SysML v2 Mapping / §3 arc42 Mapping / §4 Traceability / §5 Modeling Procedure structure used by `integration/crossmapping/`.
* **Update**: `integration/okf-ontology.md` extended with SWA-* nodes (§1.4), a Software Architecture adjacency table (§3.1), a Level 8 dependency-graph section (§4), a `software/` folder row (§6), and a new L5 row (§7.1) replacing the previous single-row L4 stub's implicit "everything below Product Architecture" scope with an explicit SWA-01..07 breakdown (L4 retained for generic component detail).
* **Update**: `integration/index.md` extended with a link to this bundle and new arc42-section-to-SWA-artifact rows.

## 2026-07-27 (audit pass)

* **Correction**: Fixed a factual error in `products/architecture-decisions.md`, `products/solution-strategy.md`, `crossmapping/architecture-decisions.md`, and `crossmapping/solution-strategy.md` — all referenced "next available ADR number after `ADR_007`" and used `ADR_010` as the illustrative next decision, but the project already has `ADR_008_BuzzerFeedback.sysml`. Corrected to reference `ADR_008` as the latest existing decision and `ADR_009` as the next available number.
* **Verification**: Audited the full bundle (16 files) against OKF v0.1 SPEC conformance rules (§9): all non-reserved `.md` files have parseable frontmatter with a non-empty `type`; `index.md`/`log.md` structure follows §6/§7. Verified all internal and cross-bundle markdown links resolve to existing files (automated link-check, zero broken links). Confirmed every project-specific fact cited (attribute names/values on `ControlMCU` and firmware `part def`s, `ADR_Common::ArchDecisionMeta` field list, `L0_Constraints` requirement pattern) against the actual `.sysml` source files.

## 2026-07-28 (dry-run walkthrough + gap fixes)

* **Verification**: Walked the full SWA-01→02→{03→{04,05},06,07} process end-to-end in a dry-run (input / internal work-product / output / inter-stage linkage per stage), cross-checked every stage's claims against the current `.sysml` model rather than only against the bundle's own prose. Two defect classes found and fixed below.
* **Correction (SysML v2 semantic error)**: `crossmapping/building-block-view.md`'s §2 Modeling Pattern code example re-declared `part def MovementSupervisorSoftware` inside a new package — SysML v2 has no partial-class/reopening mechanism, so this would have created an unrelated, differently-qualified definition rather than extending the original. Rewrote the example to use subclassification (`part def MovementSupervisorSoftwareInternal :> ProductComponents::MovementSupervisorSoftware { ... }`), mirroring the project's own established precedent (`SystemStates.sysml`'s `RollerShutterControllerWithLifecycle :> RollerShutterControllerLogicalComponents`). Corrected the same wrong "reopening" implication in the Mapping Table, Dependencies, Traceability Rule 1, and §5 Procedure steps 1–2 of the same file, and in `products/building-block-view.md`'s Representation line.
* **Correction (stale facts — removed diagnostics scope)**: The project's earlier "behavior-clarification pass" permanently removed the `DiagnosticsSoftware` firmware component and `DiagnosticState` state machine (confirmed via `SystemStates.sysml` and `Project/Model/Architecture/software/PLAN.md`), leaving exactly 7 firmware `part def`s and 1 state machine (`ControllerLifecycleState`) — but the `swarch` bundle still referenced "8 firmware part defs" and `DiagnosticsSoftware`/`DiagnosticState` in 9 locations across 8 files: `crossmapping/architecture-constraints.md`, `crossmapping/deployment-view.md` (×2), `crossmapping/solution-strategy.md`, `crossmapping/cross-cutting-concepts.md`, `products/architecture-decisions.md`, `products/building-block-view.md`, `products/cross-cutting-concepts.md` (×2), `products/runtime-view.md`, `products/deployment-view.md`. All corrected to "7 firmware part defs" / `ControllerLifecycleState`-only; the dead `products/cross-cutting-concepts.md` error-handling example was replaced with two currently-valid illustrations (`SystemStates::InstallationFaultReported`/`InstallationFault` and `MemoryManagerSoftware::lastReadValid`).
* **Correction (internal inconsistency)**: `crossmapping/architecture-constraints.md`'s own Inputs table listed Requirements (SM-PRD-10) as SWA-01's third input, but its Purpose/Role-in-Process prose, Dependencies section, Traceability Incoming Links, and `integration/okf-ontology.md`'s §4 Level-8 graph and §4.2 Dependency Table all omitted it. Wired SM-PRD-10 through consistently in all five places.
* **Verification**: Re-ran a full-bundle grep sweep for "8"/"Diagnostic"/"reopen"/"refinement" after all patches — zero remaining stale references; only legitimate uses remain (e.g. "Level 8", "8-bit MCU", arc42 "Section 8").
