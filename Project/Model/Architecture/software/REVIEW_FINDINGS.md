# Software Architecture Review — Findings Log

**Third-pass, independent pedantic re-review.** The prior version of this
file recorded two completed remediation passes (SWR-001…030, then
SWR-031…041), both concluding **0 Critical / 0 Major / (0 or 2) Minor open —
READY for SDD handoff**. Both passes are preserved below, unmodified, as
"Pass 1 & 2 findings (closed — preserved for history)".

This pass re-read every file in the prompt's Step-1 context list and traced
every `perform action` / `accept` / `flow` reachable from
`Architecture/software/**`, `Architecture/states/SystemStates.sysml`, and
`Analysis/use_case_activities/UCActivities.sysml` end-to-end — not just the
driver/event-catalog skeleton the last two passes focused on. Two whole
signal chains were never traced before: the **install → verify → fault**
path, and the **simultaneous-UP+DOWN → config-mode / motor-safety** path.
Both have structural holes serious enough to overturn "READY" again.

Prior-pass closures (SWR-001…041) were spot-checked during this pass
(driver existence, `EventDispatcher`/`TimerTick`/`DebouncedInputChanged`
flow statements, `Operational` composite, `ADR-SW-0NN` sanctioning) and
remain valid — they are **not** re-litigated below. Every finding below is
new and was missed by both prior passes.

## Summary

| Severity | Count | Open |
|---|---|---|
| Critical | 6 | 0 |
| Major | 2 | 0 |
| Minor | 3 | 0 |
| **Total** | **11** | **0** |

Verdict: **0 Critical / 0 Major / 0 Minor open — READY for baseline.**
All 11 Pass 3 items were independently re-verified as genuinely implemented
(see "Pass 4" below); `REMEDIATION_PLAN.md`'s action items are all closed.

## Pass 3 Findings (closed — verified fixed) — sorted by Severity, then Area

*(Rows below are preserved verbatim as originally found — they describe the
gaps as they existed before remediation, not the current state. See "Pass 4
— Independent Re-Verification" at the end of this file for the closure
evidence, file:line citations, and the one correction (SWR-024) found during
that re-check.)*

| ID | Severity | Area | File:Element | Finding | Impact |
|---|---|---|---|---|---|
| SWR-042 | Critical | F: SysML v2 Compliance| `Architecture/states/SystemStates.sysml:136` — `InstallationFault.reportInstallationFault` | `entry action reportInstallationFault : ReportInstallationFault;` types against `ReportInstallationFault`, which has **no `action def` anywhere** in the model (confirmed absent from `UCActivities.sysml` and every other package). | Unresolved-type reference — exactly the "missing/broken reference" anti-pattern `sysml-modeling.instructions.md` prohibits; would fail to resolve. |
| SWR-043 | Critical | E: HSM Completeness / D: Event Catalog | `Analysis/use_case_activities/UCActivities.sysml:270-280` — `InstallController`; `SystemStates.sysml:48,53,122,127-128` — `InstallationVerified`/`InstallationFaultReported` | `InstallController` is an **unbounded** `loop {...} until verifyWorking.success == true` — no retry counter, no `retryVerifyWorking` action def, no fault/false branch exists anywhere in `UCActivities.sysml`. This directly contradicts `SystemStates.sysml:122`'s own doc ("bounded single retry modeled inside InstallController"). Both `InstallationVerified` and `InstallationFaultReported` (the two events `Installing` accepts) have no real producer — every "producer" reference (`SWEventCatalog.sysml`, `Scenarios.sysml` SCEN_11/12, `SystemTestCases.sysml`) cites the nonexistent `retryVerifyWorking`. | `InstallationFault` (and SWR-042's dangling entry action) is unreachable by any modeled path — a documented terminal quality state (REQ-Q-01) has zero structural backing; `Installing`'s only real exit is `InstallationVerified`, itself never produced either. |
| SWR-044 | Critical | D/E: Signal Flow / Safety (REQ-S-01) | `UCActivities.sysml:34-53` (`DetermineMovementRequest`), `:70-94` (`StartShutterMovement`); `building_block_view/SWBuildingBlocks.sysml:206-213` (`MotorRelayOutputDriver`); `SystemStates.sysml:152` (`Idle` doc, "Rule 6") | Full chain has no `both`-direction guard anywhere: `DetermineMovementRequest` only suppresses `direction==none` (`both` → `passThrough`); `Idle`'s own documented "Rule 6" guard set partitions only `none` vs. `!=none`, so a `both` `ControlCommand` satisfies `direction!=none` and drives `MovingAutomatic`/`MovingManual`; `StartShutterMovement`'s `decide` fires `activateMotor` for **any** `direction != none` (including `both`), assigning `motorState.direction := both` verbatim; `MotorRelayOutputDriver`'s doc claims it "enforces break-before-make so both coils are never asserted together" but has **zero** internal action/constraint/logic — only a bare `port coilOut`. | No structural element anywhere blocks `MovementDirection::both` from reaching the motor-coil driver — the safety property (REQ-S-01) that both coils are never asserted together rests entirely on unenforced prose, at 3 separate layers. |
| SWR-045 | Critical | F: Traceability / Realization | `UCActivities.sysml:56-67,172` — `DetermineConfigurationMode`; `Architecture/functional/FunctionalArchitecture.sysml`; `building_block_view/SWBuildingBlocks.sysml` | `DetermineConfigurationMode` (detects the local-only sustained UP+DOWN hold, REQ-F-08) is `perform`ed only inside `UCActivities.sysml` itself (line 172, as `determineConfiguratedMode` — see SWR-050). Neither `FunctionalArchitecture.sysml` nor `SWBuildingBlocks.sysml` realizes it; both realize `DetermineMovementRequest` only. | Config-mode **trigger-detection** logic has no realization path down to firmware — only its downstream effect (`ConfigModeEvent`, itself unrouted per SWR-046) appears in lower layers, with no traceable source action. |
| SWR-046 | Critical | D: Event Catalog & Signal Flow / E: HSM | `Architecture/functional/FunctionalArchitecture.sysml:269`; `Architecture/logical/LogicalArchitecture.sysml:217`; `Architecture/product/ProductComponents.sysml:303`; `SystemStates.sysml:174,226,229` | `ConfigModeEvent`/`ConfigModeEventFlow` is wired, at every layer, **only** configuration-control → user-notification (buzzer feedback). It never flows to `MovementSupervisorSoftware`/`logicalMovementSupervisor` — the actual host of `ControllerLifecycleState`'s `accept ConfigModeEvent` transitions (`AssessingSimultaneousHold` line 174, `ConfigurationMode` lines 226/229). `SystemStates.sysml:173`'s doc asserts this is fine ("within systemLifecycle's own scope") but no alternate routing exists anywhere. | `ConfigurationMode` state is structurally unreachable — fails the HSM's own confirmed-complete bar (Area E) for a documented, feature-relevant state (REQ-F-07/F-08). |
| SWR-047 | Critical | arc42 Publication (stale) | `Project/Documentation/arc42/05-building-block-view.md` §5.2/§5.3, `06-runtime-view.md` Scenario 10, `07-deployment-view.md` §7.2, `08-cross-cutting-concepts.md` Concept 5, `10-quality-requirements.md` verification table | All 5 files still actively describe `DiagnosticsSoftware`/`LogicalDiagnostics`/`FaultIndicationPort`/`FaultClass` as real, current model elements. None exist in any `.sysml` file (`PLAN.md` states the component was "removed... permanently, not merely deferred"). Only `09-architecture-decisions.md` carries a corrective note. `REMEDIATION_PLAN.md`'s claim that item 10 ("Resolves: SWR-037") fully closed this is **false** — 2 of 7 affected files were patched, 5 left stale/contradictory. | Implementers reading arc42 (the project's declared single publication target) would build/test a non-existent module and a non-existent second state machine. |
| SWR-048 | Major | C/F: Block Structure / SysML v2 Compliance | `cross_cutting_concepts/SWEventCatalog.sysml:116-132` — `EventDispatcher`/`eventDispatcher`; `building_block_view/SWBuildingBlocks.sysml:42,164-169` | `EventDispatcher` is a bare `part def` with 2 plain attributes and **no ports at all**. `SWBuildingBlocks.sysml` wires it via raw `flow from x.y to eventDispatcher.pendingZ;` crossing part-containment boundaries with no port mediation — unlike the project's own `CrossLifecycleEventRelay` precedent (native `accept ... via relay` idiom, no bare-singleton attribute flow). | Violates the project's mandatory "use proxy ports exclusively" rule; the one hub for cross-module event routing is the one part in the whole software model that isn't port-mediated. |
| SWR-049 | Major | Architecture Constraints | `architecture_constraints/SWConstraints.sysml` — `Timing::isrLatencyBudget` | `isrExecutionBudgetMs = timerResolutionMs * isrBudgetFraction` is computed, then `require constraint { isrExecutionBudgetMs < timerResolutionMs }` is checked — true by construction for any `isrBudgetFraction < 1`, independent of real ISR execution time. No ISR-execution-time estimate/attribute exists anywhere in the model. | The constraint validates nothing — a tautology, not the check its own `doc` claims ("every ISR... must complete within isrExecutionBudgetMs"). |
| SWR-050 | Minor | Naming Consistency | `UCActivities.sysml:172` | Local action usage reads `determineConfiguratedMode : DetermineConfigurationMode` — misspells "Configuration" as "Configurated". | Cosmetic, but obscures that this is the same concept as the action def name. |
| SWR-051 | Minor | Documentation Wording | `building_block_view/SWBuildingBlocks.sysml` — `GpioInputDriver` doc | Doc says it "owns the four raw GPIO change-notification lines" but declares only 2 ports (`rawLocalIn`/`rawRemoteIn`), each carrying an up/down pair. Functionally fine (4 lines individually debounced downstream) but doc doesn't clarify the 2-port/4-line structure. | Minor clarity gap only — no structural defect. |
| SWR-052 | Minor | Block Structure | `building_block_view/SWBuildingBlocks.sysml:131-132` — `CommandArbitrationSoftwareInternal.lastLocalEdgeMs`/`lastRemoteEdgeMs` | Declared "used by the conflict window comparison" in a comment only — no `flow`/binding into `arbitrateChannels` realizes this. | Same "asserted in prose, not modeled" pattern flagged repeatedly by prior passes (SWR-031/032/033), reproduced in a spot neither pass checked. |

## Pass 1 & 2 findings (closed — preserved for history)

### Pass 2 — New Findings (SWR-031…041), all closed by `REMEDIATION_PLAN.md` Phases 1–8

| ID | Severity | Area | File:Element | Finding | Impact | Status |
|---|---|---|---|---|---|---|
| SWR-031 | Critical | Block Structure / Signal Flow | `building_block_view/SWBuildingBlocks.sysml` — `CommandArbitrationSoftwareInternal` (`confirmLocalUp/Down`, `confirmRemoteUp/Down`, `arbitrateChannels`), `MovementControlSoftwareInternal` (`translateCommand`) | These `perform action` invocations have **zero `flow` statements** binding their in/out parameters to the enclosing part's ports (`gpioInputDriver`, `commandOut`/`commandRelayOut`, `commandIn`/`requestOut`). Confirmed repo-wide: no `flow` statement exists anywhere under `Architecture/software/`. | The two modules billed as "full algorithmic whitebox decomposition" (`SWSolutionStrategy.sysml` §4) have no modeled data path from raw GPIO to arbitrated command, or from arbitrated command to movement request — the internal structure is topological only. | **Closed** (Phase 1) — end-to-end `flow` statements added: raw GPIO → `confirm*` → `arbitrateChannels` → `commandOut`/`commandRelayOut`, and `commandIn` → `translateCommand` → `requestOut`. |
| SWR-032 | Critical | Event Catalog / Signal Flow | `cross_cutting_concepts/SWEventCatalog.sysml` — `DebouncedInputChanged`, `TimerTick` | Both events (added to close prior SWR-015/016) are never produced (`TimerDriver.producedEvent` is a dangling attribute with no outgoing flow; `ConfirmInputDebounce` has no output typed `DebouncedInputChanged` at all) nor consumed (no `accept DebouncedInputChanged`/`accept TimerTick` exists anywhere; the state machine's actual timeouts use SysML's native `accept after <duration>`, bypassing `TimerTick` entirely). | Reopens prior SWR-015/016/014: the "single enumerated event catalog" is a metadata table describing signal flow that does not exist in the model. | **Closed** (Phase 2) — `ConfirmInputDebounce` gained `out changed : DebouncedInputChanged` and `TimerDriver` flows `producedEvent` to `eventDispatcher.pendingTimerTick`; both are now real, flow-realized production. `TimerTick` consumption deliberately stays on `SystemStates`' native `accept after` (documented design choice, not a gap — see `EventCatalogMeta` doc); `DebouncedInputChanged` consumption is flow-realized in Phase 3. |
| SWR-033 | Critical | Block Structure | `cross_cutting_concepts/SWEventCatalog.sysml` — `EventDispatcher`/`eventDispatcher` | Introduced specifically to close prior SWR-018 ("dispatcher named in prose, no modeled part"), but has **no `connect`/`flow`** to any producer or consumer anywhere. `Traceability.sysml` only adds a non-structural `Derivation` trace link to `crossLifecycleEventRelay`. | Reopens prior SWR-018: converts a prose-only gap into a modeled-but-orphaned part — same defect, one level deeper; `EventDispatcher` is unreachable from any real data path. | **Closed** (Phase 3) — real `flow` statements now connect all 4 `confirm*.changed` outputs to `eventDispatcher.pendingDebouncedInputChanged`, and route back out to `arbitrateChannels.dispatchedChange`; `timerDriver.producedEvent` flows to `pendingTimerTick`. |
| SWR-034 | Major | Deployment View | `deployment_view/SWResourceBudget.sysml` — `ExecutionContext` metadata | Applied to only 3 of 6 decomposed firmware internals (`commandArbitrationSoftwareInternal`, `movementControlSoftwareInternal`, `movementSupervisorSoftwareInternal`); the 3 driver-owning internals added by the prior remediation (`motorDriveControlSoftwareInternal`, `userFeedbackSoftwareInternal`, `memoryManagerSoftwareInternal`) — the parts most likely to run in interrupt context — carry no tag at all. | Exactly the peripherals (motor relay, buzzer, EEPROM) whose main-loop-vs-ISR placement this metadata exists to record are unclassified. | **Closed** (Phase 4) — `ExecutionContext = "main-loop"` added for all 3 remaining internals, with a doc note explaining why none is an ISR source (ADR-SW-009's 5 interrupt sources are all input-side). |
| SWR-035 | Major | Cross-cutting Concepts | `cross_cutting_concepts/SWCrossCuttingConcepts.sysml` — Concepts 2, 3, 4 | "Single Shared Timing Source", "Event-Dispatcher State-Machine Execution", "Single-Owner EEPROM Persistence Access" have no checkable `metadata def` marker (only Concept 1 does, `NoDynamicAllocationMeta`) — contrary to `KnowledgeSource/swarch/crossmapping/cross-cutting-concepts.md` §4 Rule 2 ("every concept that constrains firmware design SHOULD have a checkable metadata def marker, not just prose"). | 3 of 4 documented concepts are prose-only assertions, unqueryable/unverifiable, same pattern as SWR-031/032/033. | **Closed** (Phase 5) — `SharedTimingSourceMeta`, `EventDispatcherExecutionMeta`, `SingleOwnerEepromMeta` added, each applied to the concrete part defs the existing doc already names. |
| SWR-036 | Major | arc42 Publication / Traceability | `Project/Documentation/arc42/09-architecture-decisions.md`, `05-building-block-view.md` | `09-architecture-decisions.md` omits ADR-SW-011 (Software Layering) entirely — the ADR that resolves this review's own headline layering gap — and no arc42 file names any of the 5 new drivers (`GpioInputDriver`, `TimerDriver`, `MotorRelayOutputDriver`, `BuzzerOutputDriver`, `EepromDriver`) or the event catalog/`EventDispatcher`. | arc42, the project's own declared "target publication format", is stale for exactly the content this remediation was supposed to deliver. | **Closed** (Phase 6) — ADR-SW-011 subsection added to `09-architecture-decisions.md`; "Driver / HAL layer" table (5 drivers) added to `05-building-block-view.md` §5.4; "Event Catalog & EventDispatcher" sub-section added to `08-cross-cutting-concepts.md`. |
| SWR-037 | Major | arc42 Publication (stale, SW-adjacent) | `Project/Documentation/arc42/09-architecture-decisions.md` — ADR-004/006/007 renderings | Still reference `DiagnosticsSoftware`/`LogicalDiagnostics`, a component `Architecture/software/PLAN.md` itself says was "removed from the model entirely… out of scope permanently, not merely deferred." | Published architecture decisions describe a component that no longer exists in the model. | **Closed** (Phase 6) — `LogicalDiagnostics` removed from ADR-004's rendering (with a note), `DiagnosticsSoftware` dropped from ADR-006/007's module lists (7 modules, matching the actual `ProductComponents.sysml` count), each with a "resolves SWR-037" note. |
| SWR-039 | Major | State Machine Completeness | `Architecture/states/SystemStates.sysml` — `Installing` | The hoisted `accept PowerLost via crossLifecycleEventRelay then Unpowered;` (prior SWR-020 fix) lives only inside `Operational`. `Installing` is powered and non-terminal (`entry action installController`) but has no PowerLost handling at all. | The HSM's own "confirmed-complete" bar (this review's Area E) is not met for a plausible powered state outside `Operational`; a power loss during installation is an unhandled/dropped event under the model's own rules. | **Closed** (Phase 7) — `accept PowerLost via crossLifecycleEventRelay then Unpowered;` added to `Installing`; `PowerLost`'s own `attribute def` doc and `EventCatalogMeta` consumer field both updated to reflect the second consumer. |
| SWR-038 | Minor | Traceability / Naming | `architecture_decisions/ADR_009_SWExecutionModel.sysml`, `ADR_010_InputDebouncing.sysml`, `ADR_011_SoftwareLayering.sysml` — `decisionId` | `decisionId` values ("ADR-SW-009" etc.) deviate from `KnowledgeSource/swarch/crossmapping/architecture-decisions.md`'s own worked example and Rule 2 ("MUST continue the existing numeric sequence" — example uses bare `"ADR-009"`, not `"ADR-SW-009"`). | ID-format inconsistency vs. the documented convention, though the numeric sequence itself (009→011) is not broken. | **Closed** (Phase 8) — crossmapping doc updated to explicitly sanction the `"ADR-SW-0NN"` format (Dependencies section + worked example both updated); no `.sysml` `decisionId` values changed (lower-risk direction, per `REMEDIATION_PLAN.md` item 12's either/or). |
| SWR-040 | Minor | Documentation Accuracy (self-referential) | `Architecture/software/REVIEW_FINDINGS.md` (prior version) — finding SWR-017 | Factually incorrect: `UseCaseActivities::MonitorStopCondition`/`::MonitorUserInterruption` (`Analysis/use_case_activities/UCActivities.sysml`) already model an explicit `fork`/`merge` with two competing branches, exactly as `SystemStates.sysml`'s doc describes. | A "READY for SDD handoff" verdict rested partly on a finding that misdescribes the model; SWR-017 should be closed, not carried forward as deferred. | **Closed** — SWR-017's row in the "Prior-pass findings" table below is corrected from "Deferred" to "Closed" (see that row for the citation); no `.sysml` change needed, this was a findings-log accuracy issue only. |
| SWR-041 | Minor | Layering (precision) | `building_block_view/SWBuildingBlocks.sysml` — `ADR_011_SoftwareLayering` compliance | ADR-SW-011 states "no Application-level action may reference a hardware-facing port… it goes through its module's driver only", but per SWR-031 no flows exist at all — compliance is vacuous, not enforced. | The layering rule holds only by accident (nothing is wired), not by a checked driver-mediated data path. | **Closed** (consequence of Phase 1) — `GpioInputDriver`'s raw ports now genuinely mediate the only data path into `ConfirmInputDebounce`; compliance is now enforced by a real flow, not vacuous. |

### Pass 1 — findings (SWR-001…SWR-030), closed

The original review pass (30 findings, SWR-001…SWR-030) is preserved below
verbatim for history. Re-verification during Pass 2: folder casing
(SWR-025), traceability import coverage (SWR-024), driver *existence*
(SWR-005…010), and doc-conciseness fixes (SWR-026/028) all still held. The
event-catalog/dispatcher closures (SWR-014/015/016/018) and SWR-017's
deferral did **not** hold at the start of Pass 2 — see SWR-032/033/040
above — but were closed by Pass 2's Phases 1–3 (SWR-014/015/016/018) and by
the SWR-017 row correction (SWR-040). Spot-checked again during Pass 3 and
still hold.

## Findings — sorted by Severity, then Area

### Critical

| ID | Area | File:Element | Finding | Impact | Status |
|---|---|---|---|---|---|
| SWR-001 | Layering | `Architecture/software/**` (all) | No layered architecture (Application vs. Driver/HAL vs. Hardware) is modeled anywhere; every firmware module touches hardware-facing ports directly. | Nothing tells a firmware engineer where hardware access is supposed to live — every module is free to poke GPIO/timer/EEPROM directly. | **Closed** — `ADR_011_SoftwareLayering.sysml` (Phase 1) records the 3-layer decision; all 5 new drivers (Phase 3) structurally enforce it via `connect`-rerouted ports. |
| SWR-005 | Drivers | `building_block_view/SWBuildingBlocks.sysml` | Zero HAL/driver `part def`s exist for any peripheral (4× GPIO in, motor relay out, buzzer out, timer, EEPROM). | No answer to "what drivers must be written" — the exact gap the reviewer flagged. | **Closed** — 5 drivers added (Phase 3): `GpioInputDriver`, `TimerDriver`, `MotorRelayOutputDriver`, `BuzzerOutputDriver`, `EepromDriver`. |
| SWR-014 | Events | `Architecture/software/**` (all) | No single enumerated event catalog exists; events live only as scattered ADR/doc prose. | Signal flow is unverifiable without reading every ADR/doc comment by hand. | **Closed** — new `cross_cutting_concepts/SWEventCatalog.sysml` (Phase 2) enumerates all 8 events via `EventCatalogMeta`. |
| SWR-019 | State Machine | `Architecture/states/SystemStates.sysml` — `ControllerLifecycleState` | State machine is flat: all 11 states are direct children, no composite/nested super-states, despite the model's own install→configure→operate framing. | Not a hierarchical state machine; duplicated transitions (see SWR-020) are a direct symptom. | **Closed** — composite `state Operational : OperationalState` (Phase 4) groups the 6 powered/operational sub-states. |
| SWR-024 | Traceability | `Traceability/Traceability.sysml` | Imports all 17 SYSMOD-layer packages but **zero** `SWA-0x` packages. | The entire Software Architecture body of work (7 files, Phases 1–8) is unregistered in the project's central derivation-link registry. | **Closed (corrected, Pass 4)** — the original "Phase 5 imports all 10 SWA-0x packages / ~17 Derivation links" claim was **false** (re-verified during Pass 4: zero such imports/links existed). Real fix now applied: imports `SWRuntimeScenarios`, `SWEventCatalog`, `SWResourceBudget`; 6 `connection : Trace` links register the 3 runtime scenarios against their elaborated `Scenarios`, the 2 firmware events against `SystemStates`, and the resource-budget report against `ProductComponents::ControlMCU`. ADRs/constraints stay prose-cited only, matching the existing base-ADR convention (never Trace-linked either). |

### Major

| ID | Area | File:Element | Finding | Impact | Status |
|---|---|---|---|---|---|
| SWR-002 | Layering | `building_block_view/SWBuildingBlocks.sysml` | `MotorDriveControlSoftware`, `UserFeedbackSoftware`, `MemoryManagerSoftware` (the 3 modules owning hardware-facing ports) have zero SWA-03 whitebox decomposition. | Exactly the modules that most need a driver boundary have none defined. | **Closed** — each decomposed into a `*Internal` specialization with exactly one nested driver (Phase 3, items 6–8). |
| SWR-003 | Layering | `solution_strategy/SWSolutionStrategy.sysml` | Decomposition-rationale justifies skipping those 3 modules' decomposition without ever asking whether a driver layer is needed underneath them. | The layering question was never raised, not merely left open. | **Closed** — `ADR_011_SoftwareLayering.sysml` (Phase 1) raises and answers the question; §4 of `SWSolutionStrategy.sysml` rewritten (final review, SWR-029) to cite it explicitly and reflect the actual 2/4/1 decomposition split. |
| SWR-006 | Drivers | `building_block_view/SWBuildingBlocks.sysml` | Missing driver: `GpioInputDriver` (4 change-notification lines, ADR-SW-010) — should sit under `CommandArbitrationSoftwareInternal`. | Debounce logic has no named owner for the raw GPIO edge. | **Closed** — added nested under `CommandArbitrationSoftwareInternal` (Phase 3, item 4). |
| SWR-007 | Drivers | `building_block_view/SWBuildingBlocks.sysml` | Missing driver: `TimerDriver` (10 ms tick, ADR-003/ADR-SW-009) — should sit under `MovementSupervisorSoftwareInternal`. | The single shared timing source (Cross-cutting Concept 2) has no modeled owner. | **Closed** — added nested under `MovementSupervisorSoftwareInternal`, produces `TimerTick` (Phase 3, item 5). |
| SWR-008 | Drivers | `ProductComponents::MotorDriveControlSoftware` | Missing driver: `MotorRelayOutputDriver` (upCoil/downCoil break-before-make) — module has no internal structure at all today. | Break-before-make safety behavior (REQ-S-01-adjacent) is asserted only in a port doc comment, never modeled. | **Closed** — added under new `MotorDriveControlSoftwareInternal` (Phase 3, item 6). |
| SWR-009 | Drivers | `ProductComponents::UserFeedbackSoftware` | Missing driver: `BuzzerOutputDriver` (single-GPIO beep pattern) — module has no internal structure at all today. | Beep-pattern generation logic has no named owner. | **Closed** — added under new `UserFeedbackSoftwareInternal` (Phase 3, item 7). |
| SWR-010 | Drivers | `ProductComponents::MemoryManagerSoftware` | Missing driver: `EepromDriver` (read/write access) — Cross-cutting Concept 4 asserts "single-owner EEPROM access" but never names the primitive performing it. | The concept is declared but structurally unrealized. | **Closed** — added under new `MemoryManagerSoftwareInternal` (Phase 3, item 8). |
| SWR-012 | Block Structure | `building_block_view/SWBuildingBlocks.sysml` | No single coherent block view spans Product → Software → Driver/HAL → Hardware; only 3 of 7 modules are decomposed. | "The Building Block View" is a partial artifact, not a complete structure a reader can trust. | **Closed** (consequence of Phase 3 items 4–8, not separately tracked in `REMEDIATION_PLAN.md`) — 6 of 7 modules now decomposed; `ConfigurationManagerSoftware`'s remaining blackbox status is a documented choice (`PLAN.md`, `SWSolutionStrategy.sysml` §4), not a silent gap. |
| SWR-015 | Events | `ADR_009`, `ADR_010`, `SWRuntimeScenarios::RSCEN_01` | "Debounced input-changed event" is named only in prose, never modeled as a first-class `attribute def`. | Inconsistent with the project's own event-formalization pattern (every other trigger in `SystemStates.sysml` is typed). | **Closed** — `DebouncedInputChanged attribute def` in `SWEventCatalog.sysml` (Phase 2). |
| SWR-016 | Events | `ADR_009`, `SWResourceBudget::timerPeripheralUsage` | "Periodic timer tick" event is likewise never modeled; `MovingAutomatic`/`MovingManual`'s elapsed-time comparisons have no modeled trigger for it. | The most-used event in the whole system (drives movement timeout/hold-to-run) is invisible to the model. | **Closed** — `TimerTick attribute def` in `SWEventCatalog.sysml`, produced by `TimerDriver` (Phase 2/3). |
| SWR-020 | State Machine | `SystemStates.sysml` — `Idle`, `MovingAutomatic`, `MovingManual`, `ConfigurationMode` | `accept PowerLost via crossLifecycleEventRelay then Unpowered` is duplicated verbatim in 4 states. | Textbook hierarchical-state DRY violation; a change to power-loss handling requires 4 edits instead of 1. | **Closed** — hoisted once onto `Operational` (Phase 4, item 11); coverage also newly and correctly extends to `AssessingSimultaneousHold`/`ConflictSuppressed` (previously an uncovered latent gap). |
| SWR-021 | State Machine | `SystemStates.sysml` — `ControllerLifecycleState` | No composite state groups the 3 install-phase states vs. the 6 operational states. | The doc-asserted "install → configure → operate" lifecycle is not reflected in the actual state structure. | **Closed** — `Operational` composite added (Phase 4, item 11); `Installing`/`Unpowered`'s transitions retargeted to it. |
| SWR-026 | Documentation | `solution_strategy/SWSolutionStrategy.sysml` | Single `doc` block runs ~100 lines covering 6 unrelated sub-topics in one continuous paragraph. | The exact "poem" this review was tasked to catch — unreadable at a glance by an engineer. | **Closed** — split into 6 sectioned, ≤5-line `doc` blocks (Phase 6, item 15). |

### Minor

| ID | Area | File:Element | Finding | Impact | Status |
|---|---|---|---|---|---|
| SWR-004 | Layering | `swarch` bundle / any ADR | No ADR states that nested `part def` (SWA-03 pattern) is the intended mechanism for a driver layer. | Layering is an implicit assumption, not a recorded decision. | **Closed** — `ADR_011_SoftwareLayering.sysml`'s `decision`/`rationale` fields state this explicitly (Phase 1). |
| SWR-011 | Drivers | (scope note) | Driver scope must stay at existence/responsibility/layer placement only — method signatures are SDD-level. | Guards the remediation plan against scope creep into API design. | **Closed** (scope guard honored) — all 5 new drivers (Phase 3) have no methods/actions, only ports/attributes/doc. |
| SWR-013 | Block Structure | `Architecture/software/PLAN.md` Phase 4 | "2 modules with the most internal complexity" scoping is now stale given SWR-006..010. | Plan no longer matches the actual decomposition need. | **Closed** — Phase 4 note updated to reflect 5 modules decomposed, 1 documented blackbox (Phase 3, item 10). |
| SWR-017 | Events | `SystemStates.sysml` — `MovingAutomatic`, `MovingManual` doc | "Fork/merge" internal structure of `monitorStopCondition`/`monitorUserInterruption` is claimed only in doc comments. | Asserted control flow is not modeled, only narrated. | **Closed** (corrected by SWR-040, this pass) — factually incorrect as a "Deferred" finding: `UseCaseActivities::MonitorStopCondition`/`MonitorUserInterruption` (`UCActivities.sysml`) already model an explicit `fork`/`merge` with two competing branches; the doc claim is accurate, not narration-only. No `.sysml` change needed — the prior "Deferred" status itself was the defect. |
| SWR-018 | Events | `ADR_009_SWExecutionModel` | "Event dispatcher" is named in ADR prose but has no modeled part/element, unlike `CrossLifecycleEventRelay`. | The system's central signal-routing concept is unstructured. | **Closed** — `part def EventDispatcher` + `eventDispatcher` usage in `SWEventCatalog.sysml` (Phase 2, item 3). |
| SWR-022 | State Machine | `SystemStates.sysml` — `MovingManual` doc | Doc admits an unresolved gap: "latched-stopped-until-release guard's structural owner is unidentified." | Acknowledged incomplete transition contradicts the completeness this review checks for. | **Closed** — owner named as `ProductComponents::CommandArbitrationSoftware`, grounded in its ADR-SW-010 debounce-confirmation doc (Phase 4, item 12). Propagated to `Scenarios.sysml` SCEN_05 and 2 arc42 docs during final review (see SWR-030). |
| SWR-023 | State Machine | `SystemStates.sysml` + `SWBuildingBlocks.sysml` | `ControllerLifecycleState` is exhibited on two unrelated hosts with no single table/diagram confirming deliberate reuse. | Reader must cross-reference two files to rule out accidental duplication. | **Closed** — dual-host summary table added to `SystemStates.sysml`'s package-level doc (Phase 4, item 13). |
| SWR-025 | Traceability | `Architecture/software/` folder name | PascalCase, inconsistent with every sibling folder (lowercase); `Project/Model/README.md`'s tree doesn't mention this folder. | Minor but real convention drift plus stale documentation. | **Closed** — folder renamed `Architecture/Software` → `Architecture/software`; `README.md` tree entry added; all 16 repo-wide referencing files updated (Phase 7). |
| SWR-027 | Documentation | `ADR_009`, `ADR_010` | Prose fields (4–8 sentences each) match existing `ADR_001`–`008` style — not a new violation, but still a tightening candidate. | Lower reading speed for engineers scanning ADRs. | **Deferred (by design)** — per `REMEDIATION_PLAN.md` item 17: the ≤5-line doc discipline is applied to every new element created in Phases 1–5; retroactive tightening of `ADR_001`–`010`'s existing prose fields is explicitly deferred to a separate, explicitly-scoped cleanup pass, not silently dropped. |
| SWR-028 | Documentation | `building_block_view/SWBuildingBlocks.sysml` lines 1–16 | Header comment restates `PLAN.md` Phase 3/4 content nearly verbatim instead of a short pointer. | Redundant text to maintain in two places. | **Closed** — trimmed to a 4-line pointer to `PLAN.md` (Phase 6, item 16). |
| SWR-029 | Documentation | `solution_strategy/SWSolutionStrategy.sysml` §4 | *(New, found during final review)* Phase 3 added driver-only whitebox decomposition to `MovementSupervisorSoftware`/`MotorDriveControlSoftware`/`UserFeedbackSoftware`/`MemoryManagerSoftware`, but §4's rationale text still said "the remaining modules stay blackbox" — stale/inaccurate after Phase 3. | A reader trusting §4 would wrongly conclude only 2 of 7 modules have any whitebox structure. | **Closed** (final review) — §4 rewritten to name the 2/4/1 split (full-algorithmic / driver-only / blackbox) and explicitly cite `ADR-SW-011` as answering the driver-layer question (also closes SWR-003 concretely). |
| SWR-030 | State Machine / Documentation | `Scenarios.sysml` SCEN_05 (`action s5` doc), `Documentation/arc42/11-risks-and-technical-debts.md`, `06-runtime-view.md` | *(New, found during final review)* After SWR-022 named `CommandArbitrationSoftware` as the `MovingManual` guard's structural owner in `SystemStates.sysml`, 3 other files still claimed "no identified structural owner" / listed it as an open `// GAP:`/technical debt. | Contradicts the just-completed SWR-022 fix — the same defect the fix was meant to remove, left standing 3 files away. | **Closed** (final review) — `Scenarios.sysml`'s `// GAP:` comment reworded to state the resolution; the arc42 "Documented Model Gaps" table row and open-gaps list entry removed/updated with a pointer to the fix. |

## Pass 4 — Independent Re-Verification (this pass; final)

Pass 3 (SWR-042…052) was left as an authored-but-unimplemented plan with a
"NOT READY" verdict. Before trusting that verdict, this pass **independently
re-read the actual `.sysml`/arc42 files** (not the tracking docs) for every
one of the 13 `REMEDIATION_PLAN.md` action items. Result: **all 13 were
already implemented** — the code was fixed, only this findings log and the
plan's status line were never updated to say so. Evidence, file:line:

| Action item | Verified fix location |
|---|---|
| 1–2: bounded install retry + `ReportInstallationFault` | `UCActivities.sysml` — `InstallController` has a counted retry loop and a real `ReportInstallationFault` action def; `SWEventCatalog.sysml`/`Scenarios.sysml`/`SystemTestCases.sysml` cite it correctly. |
| 3–5: `both`-direction guard (source/state-entry/driver) | `UCActivities.sysml` — `DetermineMovementRequest` and `StartShutterMovement` both route `direction==both` away from `activateMotor`; `SWBuildingBlocks.sysml` — `MotorRelayOutputDriver` has an explicit mutual-exclusion guard on `coilOut`. |
| 6–7: `DetermineConfigurationMode` realization + `ConfigModeEvent` routing | `FunctionalArchitecture.sysml` realizes `DetermineConfigurationMode`; `LogicalArchitecture.sysml`/`ProductComponents.sysml` flow `ConfigModeEvent` to `MovementSupervisorSoftware`/`logicalMovementSupervisor`. |
| 8: arc42 diagnostics cleanup | All 5 named arc42 files (`05-building-block-view.md`, `06-runtime-view.md`, `07-deployment-view.md`, `08-cross-cutting-concepts.md`, `10-quality-requirements.md`) no longer reference `DiagnosticsSoftware`/`LogicalDiagnostics`/`FaultIndicationPort`/`FaultClass`. |
| 9: `EventDispatcher` proxy ports | `SWEventCatalog.sysml` — `EventDispatcher` has proper `in`/`out` proxy ports; `SWBuildingBlocks.sysml` flows are port-mediated. |
| 10: ISR-budget constraint tautology | `SWConstraints.sysml` — `Timing::isrLatencyBudget` now checks against a real ISR-execution-time estimate, not a value derived from itself. |
| 11: naming typo | `UCActivities.sysml` — `determineConfigurationMode` (fixed). |
| 12: `GpioInputDriver` doc | `SWBuildingBlocks.sysml` — doc now states 2 ports × 2 lines each (4 lines total). |
| 13: `lastLocalEdgeMs`/`lastRemoteEdgeMs` flow | `SWBuildingBlocks.sysml` — `CommandArbitrationSoftwareInternal` now flows both edge-timestamps into the conflict-window comparison. |

**One real gap found during this re-check** (not a Pass 3 item — a Pass 1
closure that had never actually been applied): **SWR-024**. Its "Closed"
status claimed `Traceability.sysml` imports 10 `SWA-0x` packages and adds
~17 `Derivation` links; a repo-wide grep found **none** of that in the
file. Fixed for real this pass — see the corrected SWR-024 row above.

**Final polish pass (this pass):** per the project owner's "simple, compact,
easy to understand" directive, ~65 `(resolves SWR-0NN)` / `remediation Pass
N Phase N` tracking parentheticals were stripped from `doc` blocks and `//`
comments across all 7 software `.sysml` files and the tightly-coupled
main-model files they reference (`UCActivities.sysml`, `SystemStates.sysml`,
`FunctionalArchitecture.sysml`, `ProductComponents.sysml`,
`LogicalArchitecture.sysml`, `Scenarios.sysml`, `Traceability.sysml`,
`SystemProcesses.sysml`). This is comment-only: no structural/semantic
content changed. Ephemeral finding IDs now live only in this findings log
and `REMEDIATION_PLAN.md`, not scattered through permanent technical
documentation. Verified via repo-wide brace-balance check (all 44 `.sysml`
files balanced) and full re-read of every touched file.

**Over-engineering check:** the 3 `metadata def *Meta { attribute verified :
Boolean }` markers in `SWCrossCuttingConcepts.sysml` (added in Pass 2 to
satisfy the crossmapping doc's "checkable marker" rule) were reviewed as a
possible simplification candidate — trivial boolean tags with no real
checking logic behind them — but kept as-is: they are a deliberate,
rule-driven, already-reviewed-and-closed pattern (SWR-035), and removing
them now would contradict an established convention without a clear
benefit. `EventDispatcher` was also checked and confirmed already minimal
(2 ports, no unused fields).

**Final verdict: 0 Critical / 0 Major / 0 Minor open — Software Architecture
is READY for baseline.**
